import { Binding } from '../types';
import { ContextBindings } from '@azure/functions';
export type TableEntity<T extends object = Record<string, any>> = T & {
    partitionKey: string;
    rowKey: string;
};
export declare class TableBinding implements Binding {
    private readonly data;
    constructor(data: TableEntity);
    toTrigger(): TableEntity<Record<string, any>>;
    toContextBinding(): ContextBindings;
    toBindingData(): TableEntity<Record<string, any>>;
}
//# sourceMappingURL=table-binding.d.ts.map